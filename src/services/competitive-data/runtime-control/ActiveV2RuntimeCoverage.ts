/**
 * Único formato coberto pelos dados Active V2 hoje — ver roster.json/
 * regulation.json. Compartilhado entre o orquestrador de shadow (Fase 3) e
 * o orquestrador de serving real, para nunca divergir sobre qual formato
 * é elegível para qualquer caminho V2.
 */
export const ACTIVE_V2_COVERED_FORMAT = 'champions_reg_m_b_doubles';
