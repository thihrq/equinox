# Equinox Web — v1.0.3
# Local development:
VITE_API_BASE_URL=http://localhost:3000

# Production / GitHub Pages:
# The GitHub Actions workflow injects the Render API URL during deploy.
# Use this manually only when building a production bundle outside CI.
# VITE_API_BASE_URL=https://equinox-api-c7zy.onrender.com
